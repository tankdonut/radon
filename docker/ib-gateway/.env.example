TWS_USERID=your_ib_username
TRADING_MODE=live
READ_ONLY_API=no
IB_LIVE_PORT=4001
IB_PAPER_PORT=4002
IB_PASSWORD_FILE=./secrets/ib_password.txt
# VNC for 2FA debugging (requires docker-compose.override.yml for port):
# VNC_SERVER_PASSWORD=<strong-password>
